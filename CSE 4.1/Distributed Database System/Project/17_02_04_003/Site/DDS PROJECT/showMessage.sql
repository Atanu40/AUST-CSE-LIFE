SET SERVEROUTPUT ON;

DECLARE

BEGIN
	DBMS_OUTPUT.PUT_LINE('Enter (2) to insert ');
	DBMS_OUTPUT.PUT_LINE('Enter (1) to insert in server ');
	DBMS_OUTPUT.PUT_LINE('Enter (2) to insert in site ');
	DBMS_OUTPUT.PUT_LINE('Enter (3) to delete ');
	DBMS_OUTPUT.PUT_LINE('Enter (1) to delete in server ');
	DBMS_OUTPUT.PUT_LINE('Enter (2) to delete in site');
END;
/